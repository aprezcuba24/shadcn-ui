---
title: BigBadge
description: Displays a big badge or a component that looks like a big badge.
component: true
---

<ComponentPreview name="bigBadge-demo" />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn-ui@latest add bigBadge
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="bigBadge" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import { BigBadge } from "@/components/ui/bigBadge"
```

```tsx
<BigBadge variant="outline">Badge</BigBadge>
```

### Link

You can use the `bigBadgeVariants` helper to create a link that looks like a big badge.

```tsx
import { bigBadgeVariants } from "@/components/ui/bigBadge"
```

```tsx
<Link className={bigBadgeVariants({ variant: "outline" })}>Big Badge</Link>
```

## Examples

### Default

<ComponentPreview name="bigBadge-demo" />

---

### Secondary

<ComponentPreview name="bigBadge-secondary" />

---

### Outline

<ComponentPreview name="bigBadge-outline" />

---

### Destructive

<ComponentPreview name="bigBadge-destructive" />
